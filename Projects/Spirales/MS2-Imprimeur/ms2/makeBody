for f in *.docx; do echo $f; \
    echo `basename $f .docx`;
     docxtotei --profile=oulipo --profiledir=/home/lou/Public/Stylesheets/profiles --apphome=/home/lou/Public/Stylesheets $f `basename $f .docx`.xml;
     perl -pi -e"s/<\/?TEI[^>]*>//e"  `basename $f .docx`.xml;
done; 
 

